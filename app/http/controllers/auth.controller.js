'use strict'
import express from 'express';
import bcrypt from 'bcrypt';
import jwt from 'jsonwebtoken';

// Models user 
import userModel from '../models/user.model';

// Exports module controller
module.exports = {

    login(req, res, next) {
    },

 
    register(req, res, next) {
        
    },

    activateAccount(req, res, next) {
    
    },

   
    sendTokenToResetPassword(req, res, next) {
       
    },

   
    validateKeyToResetPassword(req, res, next) {
       
    },

  
    setNewPassword(req, res, next) {
        
    }
}

